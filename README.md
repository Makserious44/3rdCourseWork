To set it up you must fill up the src/main/resources/hibernate.properties file with relevant data
In order to change the port you must change the port number in both src/main/resources/server.properties **AND** Client/resources/server.properties files
By default port 1610 is used
